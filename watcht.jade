head
    title watchT
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    meta(name='description' content='')
    meta(name='author' content='hab')

body
  +headerTemplate

  .map-container
    +googleMap(name="vehicleMap" options=mapOptions)

  .row
    .col-md-2
      +tripsTemplate
 
template(name='tripsTemplate')
  | Trips: #{tripCount}
  //each trips
  //  +tripTemplate

template(name='tripTemplate')
  .trip
    b #{trip_headsign}
    p Id: #{vehicle.vehicle_id} 
    p #{vehicle.vehicle_lat} #{vehicle.vehicle_lon}

template(name='headerTemplate')
  .row
    .col-xs-6
      .title Watch T

    .col-xs-6
      button.btn.btn-default.btn-md.pull-right.current-loc-btn
        .glyphicon.glyphicon-screenshot(aria-hidden="true")
  p

